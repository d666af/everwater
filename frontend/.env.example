# API base URL (default: /api — proxied by Vite to localhost:8000)
VITE_API_URL=/api

# Backend target URL for Vite proxy (dev only)
VITE_BACKEND_URL=http://localhost:8000

# Set to "true" to use mock data without backend
# VITE_MOCK=true
